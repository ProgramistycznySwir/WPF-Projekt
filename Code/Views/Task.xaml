<UserControl x:Class="WPF_Project.Task"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Project"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="350">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <GroupBox Grid.RowSpan="3" Grid.ColumnSpan="3" BorderBrush="#25A0DA" Background="#F5F5D1"></GroupBox>

        <TextBox Grid.Row="0" Grid.Column="0" Text="Name" TextWrapping="Wrap" Margin="5" />

        <Rectangle  Grid.Row="0" Grid.Column="2" Fill="{Binding Path=FillColor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinWidth="15" MinHeight="10" Stroke="Black" Margin="5"/>

        <ComboBox IsEditable="True" Text="Tags" Grid.Row="2" Grid.Column="0" Width="auto" Height="20"  SelectionChanged="ListBox_SelectionChanged">
            <ComboBoxItem>
                <CheckBox>tag1</CheckBox>
            </ComboBoxItem>
            <ComboBoxItem>
                <CheckBox>tag2</CheckBox>
            </ComboBoxItem>
        </ComboBox>
        <DatePicker Name ="datePicker1"  Grid.Row="2" Grid.Column="1" Margin="5" />
        <Button Grid.Row="2" Grid.Column="2" Content="&lt;" Margin="5" MinWidth="20" MaxWidth="30" HorizontalAlignment="Left" />
        <Button Grid.Row="2" Grid.Column="2" Content="&gt;" Margin="5" MinWidth="20" MaxWidth="30" HorizontalAlignment="Right" />
        <ComboBox IsEditable="True" Text="Subtasks" Grid.Row="1" Grid.Column="0" Width="auto" Height="20"  SelectionChanged="ListBox_SelectionChanged">
            <ComboBoxItem>
                <CheckBox>subtask1</CheckBox>
            </ComboBoxItem>
            <ComboBoxItem>
                <CheckBox>subtask2</CheckBox>
            </ComboBoxItem>
        </ComboBox>

    </Grid>
</UserControl>
